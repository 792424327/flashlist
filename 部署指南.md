# FlashList Docker 部署指南

本指南将帮助你使用 Docker 将 FlashList 应用部署到生产环境。

## 📋 目录

- [前置要求](#前置要求)
- [本地 Docker 部署](#本地-docker-部署)
- [生产服务器部署](#生产服务器部署)
- [数据库升级（推荐）](#数据库升级推荐)
- [HTTPS 配置](#https-配置)
- [维护与监控](#维护与监控)
- [故障排查](#故障排查)

---

## 前置要求

### 本地开发环境

- Docker 20.10+
- Docker Compose 2.0+
- Git

### 生产服务器

- Linux 服务器（Ubuntu 20.04+ / CentOS 8+ / Debian 11+）
- 2GB+ RAM
- 20GB+ 磁盘空间
- 公网 IP 地址
- 域名（可选，建议配置）

---

## 本地 Docker 部署

### 1. 环境变量配置

复制环境变量模板并填写：

```bash
# 复制根目录环境变量文件
cp .env.example .env

# 编辑 .env 文件
nano .env
```

**.env 文件配置**：

```bash
# 生成安全的 JWT 密钥
JWT_SECRET=$(node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")

# 设置 CORS 来源（生产环境改为实际域名）
CORS_ORIGIN=http://localhost
```

### 2. 构建并启动服务

```bash
# 构建 Docker 镜像
docker-compose build

# 启动服务（后台运行）
docker-compose up -d

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

### 3. 验证部署

```bash
# 检查后端健康状态
curl http://localhost:3001/health

# 检查前端
curl http://localhost/health

# 访问前端页面
# 浏览器打开: http://localhost
```

### 4. 停止服务

```bash
# 停止服务
docker-compose down

# 停止服务并删除数据卷
docker-compose down -v
```

---

## 生产服务器部署

### 步骤 1: 服务器准备

#### 1.1 安装 Docker

**Ubuntu/Debian**:
```bash
# 更新软件包
sudo apt-get update

# 安装依赖
sudo apt-get install -y ca-certificates curl gnupg lsb-release

# 添加 Docker 官方 GPG 密钥
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# 添加 Docker 仓库
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker Engine
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 启动 Docker 服务
sudo systemctl start docker
sudo systemctl enable docker

# 验证安装
docker --version
docker compose version
```

**CentOS/RHEL**:
```bash
# 安装依赖
sudo yum install -y yum-utils

# 添加 Docker 仓库
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# 安装 Docker
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 启动 Docker
sudo systemctl start docker
sudo systemctl enable docker
```

#### 1.2 配置防火墙

```bash
# Ubuntu (UFW)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 22/tcp  # SSH
sudo ufw enable

# CentOS (firewalld)
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

### 步骤 2: 上传代码到服务器

#### 方法 1: 使用 Git（推荐）

```bash
# 在服务器上克隆项目
cd /opt
sudo git clone https://your-git-repo-url.git flashlist
cd flashlist

# 设置正确的权限
sudo chown -R $USER:$USER /opt/flashlist
```

#### 方法 2: 使用 SCP/SFTP

```bash
# 在本地打包项目（排除不必要的文件）
tar -czf flashlist.tar.gz \
  --exclude=node_modules \
  --exclude=.git \
  --exclude=data \
  --exclude=logs \
  flashlist/

# 上传到服务器
scp flashlist.tar.gz user@your-server:/opt/

# 在服务器上解压
ssh user@your-server
cd /opt
tar -xzf flashlist.tar.gz
cd flashlist
```

### 步骤 3: 配置生产环境变量

```bash
# 创建 .env 文件
cp .env.example .env
nano .env
```

**生产环境 .env 配置**：

```bash
# JWT 密钥（使用强随机密钥）
JWT_SECRET=<生成的64位随机字符串>

# CORS 来源（改为实际域名）
CORS_ORIGIN=https://your-domain.com

# 如果使用 Gemini API
GEMINI_API_KEY=your-actual-api-key
```

**生成安全的 JWT 密钥**：
```bash
# 方法 1: 使用 Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# 方法 2: 使用 OpenSSL
openssl rand -hex 32

# 方法 3: 使用 /dev/urandom
head -c 32 /dev/urandom | xxd -p -c 32
```

### 步骤 4: 部署应用

```bash
# 构建镜像
docker compose build

# 启动服务（后台运行）
docker compose up -d

# 查看服务状态
docker compose ps

# 查看日志
docker compose logs -f backend
docker compose logs -f frontend
```

### 步骤 5: 验证部署

```bash
# 检查容器状态
docker compose ps

# 检查后端健康
curl http://localhost:3001/health

# 检查前端健康
curl http://localhost/health

# 检查服务可访问性（从外部）
curl http://your-server-ip/health
```

---

## 数据库升级（推荐）

生产环境建议使用 PostgreSQL 或 MySQL 替代 SQLite。

### 使用 PostgreSQL

#### 1. 修改 docker-compose.yml

在 `docker-compose.yml` 中添加 PostgreSQL 服务：

```yaml
services:
  # 添加 PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: flashlist-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=flashlist
      - POSTGRES_USER=flashlist
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - flashlist-network
    healthcheck:
      test: ["CMD-SINGLE", "pg_isready", "-U", "flashlist"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    # ... 其他配置保持不变
    environment:
      # 修改数据库连接
      - DATABASE_URL=postgresql://flashlist:${DB_PASSWORD}@postgres:5432/flashlist?schema=public
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:
    driver: local
```

#### 2. 修改 .env 文件

```bash
# 添加数据库密码
DB_PASSWORD=your-secure-database-password
```

#### 3. 重新部署

```bash
# 停止现有服务
docker compose down

# 启动新服务（包括 PostgreSQL）
docker compose up -d

# 查看数据库初始化日志
docker compose logs -f postgres
```

---

## HTTPS 配置

使用 Let's Encrypt 免费 SSL 证书。

### 方法 1: 使用 Certbot（推荐）

#### 1. 安装 Certbot

```bash
# Ubuntu/Debian
sudo apt-get install certbot

# CentOS
sudo yum install certbot
```

#### 2. 停止前端容器（临时）

```bash
docker compose stop frontend
```

#### 3. 获取 SSL 证书

```bash
sudo certbot certonly --standalone \
  -d your-domain.com \
  -d www.your-domain.com \
  --email your-email@example.com \
  --agree-tos
```

证书将保存在 `/etc/letsencrypt/live/your-domain.com/`

#### 4. 修改 Nginx 配置

编辑 `flashlist---极简大纲清单_前端/nginx.conf`：

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    # HTTP 重定向到 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;

    # SSL 证书配置
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    # SSL 安全配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    root /usr/share/nginx/html;
    index index.html;

    # ... 其余配置保持不变
}
```

#### 5. 挂载证书到容器

修改 `docker-compose.yml` 中的前端服务：

```yaml
frontend:
  # ... 其他配置
  volumes:
    - /etc/letsencrypt:/etc/letsencrypt:ro
```

#### 6. 重新构建并启动

```bash
# 重新构建前端
docker compose build frontend

# 启动服务
docker compose up -d

# 验证 HTTPS
curl https://your-domain.com/health
```

#### 7. 设置证书自动续期

```bash
# 添加续期任务到 crontab
sudo crontab -e

# 添加以下行（每天凌晨2点检查并续期）
0 2 * * * certbot renew --quiet && docker compose restart frontend
```

### 方法 2: 使用 Nginx Proxy Manager

更简单的图形化 HTTPS 管理方案，请参考 [Nginx Proxy Manager 文档](https://nginxproxymanager.com/)。

---

## 维护与监控

### 日志管理

```bash
# 查看实时日志
docker compose logs -f

# 查看特定服务日志
docker compose logs -f backend
docker compose logs -f frontend

# 查看最近100行日志
docker compose logs --tail=100

# 保存日志到文件
docker compose logs > logs/docker-$(date +%Y%m%d).log
```

### 数据备份

#### 备份 SQLite 数据库

```bash
# 创建备份目录
mkdir -p backups

# 备份数据库
docker compose exec backend sh -c \
  "sqlite3 /app/data/prod.db .dump" > backups/backup-$(date +%Y%m%d-%H%M%S).sql

# 或者直接复制数据库文件
cp data/prod.db backups/prod-$(date +%Y%m%d-%H%M%S).db
```

#### 备份 PostgreSQL 数据库

```bash
docker compose exec postgres pg_dump -U flashlist flashlist \
  > backups/backup-$(date +%Y%m%d-%H%M%S).sql
```

#### 定时备份脚本

创建 `backup.sh`:

```bash
#!/bin/bash
BACKUP_DIR="/opt/flashlist/backups"
DATE=$(date +%Y%m%d-%H%M%S)

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
docker compose exec -T postgres pg_dump -U flashlist flashlist \
  > $BACKUP_DIR/db-$DATE.sql

# 压缩备份
gzip $BACKUP_DIR/db-$DATE.sql

# 删除30天前的备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +30 -delete

echo "备份完成: $BACKUP_DIR/db-$DATE.sql.gz"
```

设置定时任务：

```bash
chmod +x backup.sh

# 添加到 crontab（每天凌晨3点备份）
0 3 * * * /opt/flashlist/backup.sh >> /opt/flashlist/logs/backup.log 2>&1
```

### 更新应用

```bash
# 拉取最新代码
git pull

# 重新构建镜像
docker compose build

# 重启服务（零停机更新）
docker compose up -d --no-deps --build backend
docker compose up -d --no-deps --build frontend

# 或者完全重启
docker compose down
docker compose up -d
```

### 清理资源

```bash
# 清理未使用的镜像
docker image prune -a

# 清理未使用的容器
docker container prune

# 清理未使用的卷
docker volume prune

# 清理所有未使用的资源
docker system prune -a --volumes
```

---

## 故障排查

### 容器无法启动

```bash
# 查看容器状态
docker compose ps

# 查看详细错误日志
docker compose logs backend
docker compose logs frontend

# 检查容器内部
docker compose exec backend sh
docker compose exec frontend sh
```

### 数据库连接失败

```bash
# 检查数据库服务状态
docker compose ps postgres

# 查看数据库日志
docker compose logs postgres

# 手动测试数据库连接
docker compose exec backend sh
# 在容器内执行
npx prisma db push
```

### 端口被占用

```bash
# Windows
netstat -ano | findstr :80
netstat -ano | findstr :3001

# Linux
lsof -i :80
lsof -i :3001

# 修改端口（编辑 docker-compose.yml）
ports:
  - "8080:80"  # 使用 8080 替代 80
```

### 前端无法访问后端 API

1. 检查 Nginx 配置中的代理设置
2. 确认 `docker-compose.yml` 中服务在同一网络
3. 检查后端服务健康状态
4. 查看浏览器控制台的网络请求

### 数据丢失

使用备份恢复：

```bash
# SQLite
cat backups/backup-YYYYMMDD.sql | docker compose exec -T backend \
  sh -c "sqlite3 /app/data/prod.db"

# PostgreSQL
cat backups/backup-YYYYMMDD.sql | docker compose exec -T postgres \
  psql -U flashlist -d flashlist
```

### 内存不足

```bash
# 限制容器内存使用（修改 docker-compose.yml）
services:
  backend:
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M
```

---

## 性能优化建议

### 1. 启用 Redis 缓存（可选）

在 `docker-compose.yml` 添加 Redis 服务用于会话管理和缓存。

### 2. 配置 CDN

将静态资源上传到 CDN（如阿里云 OSS、腾讯云 COS）。

### 3. 数据库优化

- 定期清理旧数据
- 添加合适的索引
- 使用连接池

### 4. 监控工具

推荐使用：
- **Portainer**: Docker 容器管理界面
- **Prometheus + Grafana**: 性能监控
- **Sentry**: 错误追踪

---

## 安全检查清单

- [ ] 修改默认的 JWT_SECRET
- [ ] 使用强密码保护数据库
- [ ] 配置 HTTPS（生产环境必须）
- [ ] 限制 CORS 来源为实际域名
- [ ] 定期更新 Docker 镜像
- [ ] 配置防火墙规则
- [ ] 启用数据库备份
- [ ] 设置日志轮换
- [ ] 定期检查安全更新

---

## 常用命令速查

```bash
# 启动服务
docker compose up -d

# 停止服务
docker compose down

# 查看状态
docker compose ps

# 查看日志
docker compose logs -f

# 重启服务
docker compose restart

# 重新构建
docker compose build

# 进入容器
docker compose exec backend sh
docker compose exec frontend sh

# 查看资源使用
docker stats

# 备份数据
cp -r data backups/data-$(date +%Y%m%d)

# 更新应用
git pull && docker compose up -d --build
```

---

## 获取帮助

- **项目文档**: 查看 `快速开始.md` 和 `上下文定义/` 目录
- **Docker 文档**: https://docs.docker.com/
- **问题反馈**: 在项目 GitHub Issues 提交问题

---

🎉 **恭喜！你已经成功完成 FlashList 的 Docker 部署！**

祝你使用愉快！
